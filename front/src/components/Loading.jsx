import React from 'react';

const Loading = () => {
    return (
        <div className="loading-container">
            <div className="spinner">
                {/* Optional: Add content inside the spinner here */}
            </div>
        </div>
    );
};

export default Loading;
